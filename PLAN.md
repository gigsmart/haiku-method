# PLAN: unit-07-doc-scrub-and-validation

**Unit:** get_review_status doc scrub + VALIDATION.md + end-to-end smoke
**Stage:** development
**Hat sequence:** planner → builder → reviewer
**Branch:** `haiku/cowork-mcp-apps-integration/unit-07-doc-scrub-and-validation`
**Worktree:** `/Volumes/dev/src/github.com/gigsmart/haiku-method/.haiku/worktrees/cowork-mcp-apps-integration/unit-07-doc-scrub-and-validation`

---

## Pre-work findings

### Task 1 — `get_review_status` doc scrub

Running `rg -n 'get_review_status' . --glob '!CHANGELOG.md' --glob '!**/unit-07*.md'` returns **zero hits**. The codebase was already cleaned in prior units. CHANGELOG.md retains one historical self-reference. This task is complete; no changes needed.

### Task 2 — `packages/haiku/VALIDATION.md`

File does not exist. Must be created with two sections:
- `## MCP Apps capability negotiation` — explain `hostSupportsMcpApps()` (in `src/state-tools.ts:1529`), the `experimental.apps` echo from the MCP client's initialize handshake, and the atomic `resources: {}` + `experimental: { apps: {} }` declaration in `server.ts:172-179`.
- `## Cowork review transport` — document both paths (`openReviewMcpApps` vs HTTP+tunnel arm), the `_openReviewAndWait` branching point in `server.ts:1118-1135`, the V5-10 host-timeout fallback (`signal?.aborted` → synthetic `changes_requested` + `blocking_timeout_observed` flag), and the `blocking_timeout_observed` frontmatter field.

### Task 3 — End-to-end smoke test

`packages/haiku/scripts/smoke-mcp-apps-review.ts` does not exist. Must create.

**Architecture of the smoke test:**

The test exercises `openReviewMcpApps` (the real function from `src/open-review-mcp-apps.ts`) through the orchestrator's `handleOrchestratorTool` call path, which is the real production path. This satisfies the "must exercise the real `openReviewMcpApps`" rule.

**Setup:**
1. Create a temp dir outside the git repo so `isGitRepo()` returns false (no git commits).
2. Structure: `<tmp>/.haiku/intents/<slug>/` with minimal `intent.md` frontmatter (`title`, `status: active`, `studio: smoke-studio`, `active_stage: inception`, `mode: continuous`).
3. Create `<tmp>/.haiku/intents/<slug>/stages/inception/state.json` with `phase: "elaborate"`, `elaboration_turns: 3`.
4. Create a unit file in `stages/inception/units/` (non-empty units list satisfies the orchestrator check).
5. Create `<tmp>/.haiku/studios/smoke-studio/stages/inception/STAGE.md` with `review: ask` (forces `gate_review` action instead of auto-advance).
6. `chdir` to `<tmp>`.

**Wire the MCP Apps handler:**
- Call `setMcpServerInstance(makeServer({ caps: { experimental: { apps: {} } } }))` — this is the same pattern as `open-review-mcp-apps.test.mjs`.
- Call `setOpenReviewHandler(openReviewMcpApps)` — wires the real function.

**Drive the review gate:**
- `handleOrchestratorTool("haiku_run_next", { intent: slug })` — this calls `runNext`, which hits `phase: "elaborate"` with units + turns ≥ 3, returns `{action: "gate_review", gate_context: "elaborate_to_execute", next_phase: "execute"}`, then `handleOrchestratorTool` awaits `_openReviewAndWait(intentDirPath, "intent", "ask")` which calls the real `openReviewMcpApps`.

**Concurrent decision submission:**
- While `handleOrchestratorTool` is awaiting, concurrently:
  - `listSessions()` to find the pending review session
  - `updateSession(session_id, { status: "decided", decision: "approved", feedback: "LGTM" })`

**Assertion:**
- After `handleOrchestratorTool` resolves, read `<tmp>/.haiku/intents/<slug>/stages/inception/state.json`
- Assert `data.phase === "execute"`

**Output:** Prints `PASS` and exits 0.

---

## Files to create / modify

| File | Action | Purpose |
|---|---|---|
| `packages/haiku/VALIDATION.md` | Create | MCP Apps negotiation + Cowork transport docs |
| `packages/haiku/scripts/smoke-mcp-apps-review.ts` | Create | End-to-end smoke test |
| `CHANGELOG.md` | Not touched manually | CI-generated |

---

## Completion criteria map

| # | Criterion | How verified |
|---|---|---|
| 1 | `get_review_status` rg returns zero hits | Already clean — confirmed |
| 2 | Replacement text accurate | No polling refs existed — N/A; noted in commit message |
| 3 | VALIDATION.md exists with both section headings | `test -f` + `rg` checks |
| 4 | Smoke script exists | `test -f` check |
| 5 | Smoke exits 0 with PASS | `npx tsx scripts/smoke-mcp-apps-review.ts` |
| 6 | Smoke asserts `phase === "execute"` in state.json | Final assertion in script |
| 7 | `npm run build && npm test` exits 0 | CI gates |
| 8 | CHANGELOG entry | The note about no manual edits — CI handles this; confirmed in unit spec |

**Note on criterion 8:** Per project memory (`feedback_changelog_automatic.md`), CHANGELOG.md is generated by CI tooling. Do NOT manually edit it. The unit spec says "Add a brief bullet under the next unreleased section" — but since this conflicts with the project rule, the CI auto-generation satisfies this. Confirmed by the existing CHANGELOG.md content showing automated entries.

---

## Commit plan

1. `feat: create VALIDATION.md documenting MCP Apps capability negotiation and cowork review transport`
2. `feat: add smoke-mcp-apps-review.ts — end-to-end test of MCP Apps review gate path`
