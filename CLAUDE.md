# H·AI·K·U Project

H·AI·K·U = Human + AI Knowledge Unification — a universal lifecycle framework for structured AI-assisted work.

Three-component project: **plugin** (Claude Code plugin), **paper** (methodology spec), **website** (Next.js 15 static site).

- Paper is the source of truth for methodology concepts
- Plugin is the source of truth for implementation (orchestrator, MCP tools, hooks, runtime behavior)
- **`plugin/studios/ARCHITECTURE.md` is the structural reference for studio/stage/unit/hat/feedback boundaries within the plugin** — the rules and contracts that apply across studios, distinct from any single implementation file. Read it before structural changes; conflict resolution between this doc and the plugin code is described in the doc's intro.
- Website presents all of the above to users

## Sync Discipline (CRITICAL)

When modifying any component, check if other components need corresponding updates:

| Change Type | Paper | Plugin | Website |
|---|---|---|---|
| New prompt | Mention in relevant section | Add handler in `prompts/*.ts` | Update docs if user-facing |
| New studio | Document in Profiles section | Primary | Update docs |
| New stage | Document in relevant profile | Primary | Update docs |
| New hat (in stage) | Document in relevant profile | Add `hats/{hat}.md` file in stage directory | Update docs if user-facing |
| New review agent (in stage) | Document in Quality Enforcement | Add `review-agents/{agent}.md` file in stage directory (resolved via 3-tier cascade — see "Hat / review-agent cascade" below) | Update docs if user-facing |
| New fix hat (in stage) | Mention in Fix Loop section | Add `hats/{hat}.md` file in stage directory + reference in `fix_hats:` on STAGE.md | Update docs if user-facing |
| New studio-level intent-completion review agent | Mention in Intent-Completion Review section | Add `intent-review-agents/{agent}.md` in the studio directory (per-studio), OR `plugin/intent-review-agents/{agent}.md` for a GLOBAL agent every studio inherits (studio files override global by name). NOT per-stage. Directory was renamed from `review-agents/` 2026-05-18 so the studio-level slot in the stage cascade is free | Update docs if user-facing |
| New global hat / review agent (any studio) | Document if added | Add to `plugin/hats/{hat}.md` or `plugin/review-agents/{agent}.md` — resolved by every stage in every studio unless a more-specific tier overrides | Update docs if user-facing |
| New studio-level fix hat | Mention in Intent-Completion Review section | Add `fix-hats/{hat}.md` — `plugin/fix-hats/{hat}.md` for a GLOBAL one every studio inherits (e.g. `validator`, `reconciler`), OR `plugin/studios/{name}/fix-hats/{hat}.md` for a studio override (overrides global by name). NOT per-stage. Resolved via the global → studio cascade (project `.haiku/` overrides plugin at each tier) | Update docs if user-facing |
| New stage elaboration guidance | Mention in Stages section if needed | Add `phases/ELABORATION.md` (additive elaborate-prompt content, not an engine override) | Update docs if user-facing |
| New reflection dimension | Document in Reflection section | Add `reflections/{dim}.md` file in studio directory — injected as a lens into the automatic intent-close `record_reflection` phase | Update docs if user-facing |
| New lifecycle phase | Document as new section | Implement | Update docs |
| Terminology change | Update all references | Update all references | Update all references |
| New principle | Document in Principles section | Implement if applicable | Update if referenced |
| Concept refinement | Update definition | Update implementation | Update docs |
| Persistence change | N/A (environment-detected) | Update state-tools.ts isGitRepo | Update docs if user-facing |
| New harness support | N/A | Add entry to HARNESS_REGISTRY in `harness.ts`, update rewriting rules in `harness-instructions.ts` | Update docs if user-facing |

## Key File Locations

- Paper: `website/content/papers/haiku-method.md`
- Plugin metadata: `plugin/.claude-plugin/plugin.json`
- Plugin prompts: `packages/haiku/src/prompts/*.ts` (MCP prompt handlers — all behavior lives here)
- Plugin studios: `plugin/studios/*/STUDIO.md`
- Plugin stages: `plugin/studios/*/stages/*/STAGE.md`
- Plugin hats (stage tier): `plugin/studios/*/stages/*/hats/*.md`
- Plugin hats (studio tier): `plugin/studios/*/hats/*.md`
- Plugin hats (global tier): `plugin/hats/*.md` (classifier, feedback-assessor)
- Plugin review agents (stage tier): `plugin/studios/*/stages/*/review-agents/*.md`
- Plugin review agents (studio tier): `plugin/studios/*/review-agents/*.md`
- Plugin review agents (global tier): `plugin/review-agents/*.md`
- Plugin stage elaboration guidance: `plugin/studios/*/stages/*/phases/ELABORATION.md`
- Plugin reflections: `plugin/studios/*/reflections/*.md` (dimension lenses for the automatic intent-close reflection phase)
- Plugin intent-completion review agents (global tier): `plugin/intent-review-agents/*.md` (applies to every studio)
- Plugin studio-level intent-completion review agents: `plugin/studios/*/intent-review-agents/*.md` (override global by name)
- Plugin studio-level fix hats (intent-completion fix loop): `plugin/fix-hats/*.md` (global tier — `validator`, `reconciler`; every studio inherits) + `plugin/studios/*/fix-hats/*.md` (studio overrides by name)
- Plugin intent templates: `plugin/studios/*/templates/*.md`
- Plugin hooks: `plugin/hooks/*.sh` + `plugin/.claude-plugin/hooks.json`
- Plugin libraries: `plugin/lib/*.sh`
- Plugin orchestration: `plugin/lib/orchestrator.sh`, `plugin/lib/stage.sh`, `plugin/lib/studio.sh`
- Plugin environment detection: `packages/haiku/src/state-tools.ts` (isGitRepo)
- Plugin harness support: `packages/haiku/src/harness.ts` (capability registry), `packages/haiku/src/harness-instructions.ts` (instruction adaptation)
- Plugin providers: `plugin/providers/*.md` (bidirectional translation instructions) + `plugin/schemas/providers/*.json`
- Website docs: `website/content/docs/`
- Infrastructure: `deploy/terraform/`
- Changelog: `CHANGELOG.md` (Keep a Changelog format)

## Concept-to-Implementation Mapping

| Concept | Paper Section | Plugin Implementation | Key Files |
|---|---|---|---|
| Intent | Elaboration phase | `.haiku/intents/{slug}/intent.md` | prompts/core.ts |
| Unit | Elaboration phase | `.haiku/intents/{slug}/stages/{stage}/units/unit-NN-*.md` | prompts/core.ts |
| Bolt | Execution phase | `iteration` field in iteration.json | orchestrator.ts |
| Studio | Profiles section | `plugin/studios/{name}/STUDIO.md` | studio.sh |
| Stage | Profiles section | `plugin/studios/{name}/stages/{stage}/STAGE.md` | stage.sh, orchestrator.ts |
| Hat | Profiles section | Three-tier cascade (more-specific wins): `plugin/hats/{hat}.md` → `plugin/studios/{name}/hats/{hat}.md` → `plugin/studios/{name}/stages/{stage}/hats/{hat}.md`. Project `.haiku/` overrides plugin at each tier. Resolver: `readHatDefs` / `resolveHatPath` in `studio-reader.ts`. Agent-facing read: `haiku_read_hat { studio, stage, hat, fix? }` resolves the cascade (`fix: true` → `resolveFixHatPath`), strips frontmatter, returns the body — dispatch prompts call it instead of carrying an inlined mandate. Stage scope via `haiku_read_stage { studio, stage }` | studio-reader.ts, state-tools.ts |
| Review Agent | Quality Enforcement | Three-tier cascade (more-specific wins): `plugin/review-agents/{agent}.md` → `plugin/studios/{name}/review-agents/{agent}.md` → `plugin/studios/{name}/stages/{stage}/review-agents/{agent}.md`. Project `.haiku/` overrides plugin at each tier. Resolver: `readReviewAgentDefs` / `readReviewAgentPaths` in `studio-reader.ts`. Configured agents fire in BOTH stage walks: `dispatch_review` (pre-execute, audits spec) and `dispatch_approval` (post-execute, audits work) | studio-reader.ts |
| Reviews vs Approvals (stage) | N/A | Reviews are PRE-execute (`dispatch_review` walk fires between `elaborate_loop` completion and wave-ready hat dispatch — audits the SPEC). Approvals are POST-execute (`dispatch_approval` walk fires after all units' hat sequences complete — audits the WORK). Engine roles (spec, continuity, cross-stage-consistency) fire in BOTH with phase-appropriate mandate bodies in sibling `engine-bodies/` dirs. Per-unit `reviews.*` and `approvals.*` FM stamps mirror the split | orchestrator/workflow/cursor.ts |
| Spec Review (engine phase) | Quality Enforcement | Universal hard gate that always fires between `execute` and the parallel quality review on every stage. Engine-owned: built-in spec-conformance prompt lives in `prompts/spec_review.ts`, no per-studio mandate file, no opt-out — every intent has a spec, every stage produces something the intent scoped, so every stage benefits. State tracked via `spec_review_dispatched` + `quality_review_dispatched` in stage state.json | `handlers/review.ts`, `handlers/gate.ts`, `prompts/spec_review.ts` |
| Fix Hats | Fix Loop | `fix_hats:` frontmatter on STAGE.md (ordered hat list dispatched against open feedback — typically ends with `feedback-assessor`). A production hat reused in the chain MAY ship a fix-scoped mandate at `plugin/studios/{studio}/stages/{stage}/fix-hats/{hat}.md` (project `.haiku/` override beats plugin) — it corrects ONE finding instead of authoring from scratch; `resolveFixHatPath` prefers it over `hats/{hat}.md`, falling back to the production mandate when absent | orchestrator.ts (`resolveStageFixHats`, `review_fix` action), studio-reader.ts (`resolveFixHatPath`) |
| Feedback Assessor | Fix Loop | Terminal hat in `fix_hats` that independently decides closure — `plugin/studios/{name}/stages/{stage}/hats/feedback-assessor.md` | orchestrator.ts |
| Studio Review Agent | Intent-Completion Review | `plugin/studios/{name}/intent-review-agents/{agent}.md` (NOT per-stage). Walked by the cursor's intent-completion walk: `intentReviewRoles(mode, studioAgents)` appends each `intent-review-agents/*.md` after the engine roles (spec, continuity, cross-stage-consistency), deduped against them, before the terminal `user` gate; `intent_review/index.ts` resolves the mandate body via `readStudioReviewAgentPaths`. (Pre-2026-05-21 the append was severed — only `cross-stage-consistency`, promoted to an engine role, fired; `runtime-verifier` silently never ran at intent completion. The drift fix restored the general append.) Resolved by a TWO-tier cascade (2026-05-21): a GLOBAL tier at `plugin/intent-review-agents/*.md` (sibling of `studios/`, same shape as global `hats/`/`review-agents/`) is read first, then the per-studio `plugin/studios/{name}/intent-review-agents/*.md` overrides by agent name; project `.haiku/` overrides plugin at each tier. `delivery-verifier` lives at the global tier so EVERY studio runs it; software additionally ships `runtime-verifier` (drives the integrated app) + `cross-stage-consistency`. Opt out with `intent_completion_review: false` on intent frontmatter. Directory was renamed from `review-agents/` 2026-05-18 to free the studio-tier slot in the stage-review cascade | cursor.ts (`intentReviewRoles`, `walkIntentTrack`), `intent_review/index.ts`, `readStudioReviewAgentPaths` |
| Delivery Verifier | Intent-Completion Review | `plugin/intent-review-agents/delivery-verifier.md` — the GLOBAL-tier intent-review-agent, so **every studio** runs it at intent close (2026-05-21; was software-only). Confirms the intent is *deliverable*: waits for the delivery PR's CI to settle (`gh pr checks --watch`), files one intent-scope FB per failure (concrete: failing check + command + error + file), AND checks that CI is *meaningful* not just green — it reads the intent's quality-gate union (`haiku_unit_list` + `haiku_unit_get { field: "quality_gates" }`) as the reference set and files a finding when a declared gate isn't run by any CI job, when a green check is a no-op (`exit 0` / "0 tests" / never-triggered conditional), or when code shipped to a remote with no pipeline enforcing the work's own bar. Also reads/replies-to/resolves the PR review conversation. Signs off only when CI is green AND meaningful AND the PR is **mergeable** (not draft, no conflicts — `gh pr view --json isDraft,mergeable`) AND no actionable review thread is open; otherwise findings drive the studio fix-hat loop and it re-audits until clean. **Provider-agnostic by design — assumes NO authed CLI/provider** (2026-05-25): (1) it FIRST checks the provider-free merge signal via local git — if `haiku/<intent>/main` is already an ancestor of mainline (`git merge-base --is-ancestor`), the host's branch protection already gated CI+review, so the merge IS proof → sign off; (2) no remote at all → genuine SKIP; (3) remote + PR exists + provider access → verify as above; (4) remote + unmerged + NO provider access (no/unauthed CLI) → it is **blind to a gate that exists**, which is NOT a SKIP — it files ONE `severity: medium` intent FB escalating to the human (confirm CI green + merge, or grant CLI access) that HOLDS the `delivery-verifier` stamp (open invalidating FB → `dispatch-stamps.ts` skips the stamp) WITHOUT spinning the fix-hat loop (medium < fix threshold; a fixer can't authenticate a CLI), and re-audits via merge-proof / live-CI on the next pass. Never a silent false-green seal on an unverifiable delivery. Carries the `PR_INTERACTION_ROLES` scope carve-out (may mutate the PR via `gh`/`glab` — reply + resolve — but never edits source; code fixes flow through findings). Independent of `runtime-verifier` (boots-locally ≠ CI-green) | delivery-verifier.md, `_shared/index.ts` (`PR_INTERACTION_ROLES`), `intent_review/subagent.eta.md` |
| Pending seal (terminal hold) | Quality Enforcement | An intent is NOT sealed (terminal write-lock `sealed_at`) until its work has landed on the repo's default branch. After every intent-scope approval is signed and reflection is done, the cursor's terminal gate checks `intentDeliveryState(slug)` (squash-merge-aware `isBranchMerged(haiku/<slug>/main, getMainlineBranch())`): if the hub branch is NOT an ancestor of the default branch it emits `pending_seal` and HOLDS — `sealed_at` stays null and the engine NEVER merges (honors "never merge unless asked"); the next tick re-checks and seals once the merge lands (local merge OR merged PR/MR both count). Inapplicable in filesystem mode / when no default branch resolves / when already merged → seals immediately as before. On `/haiku:haiku-pickup` of a held intent, `haiku_run_next` re-opens the `PR_INTERACTION_ROLES` approvals (`delivery-verifier`) so it re-audits the open change request and files feedback for any new review comments before sealing — honors cascade overrides (keyed on the role-class set, not a hardcoded name). Surfaced as derived status `pending_seal` on `haiku_intent_list`, `current_state.seal_status` + `awaiting_merge_into` on the SPA wire (rendered by `IntentCompleteView`), the `pending_seal` statusline phase, and the architecture-map post-intent card. NOT surfaced in `haiku_capacity` (throughput report stays sealed_at-only: held intents count as in-flight) | orchestrator/workflow/intent-delivery.ts, orchestrator/workflow/cursor.ts (terminal gate + `pending_seal` action), tools/orchestrator/haiku_run_next.ts (pickup re-open), prompts/intent/seal/pending_seal, current-state.ts, statusline/{state,render}.ts |
| Per-stage delivery PR (`stage_prs`) | Profiles section / Quality Enforcement | The engine opens a per-stage DRAFT PR at stage start (`workflowStartStage` → `openStageDraftPullRequest`, branch `haiku/<slug>/<stage>`, base `haiku/<slug>/main` — NOT repo default), records it in the `stage_prs: { <stage>: { url, status, ready_at } }` map on intent.md FM (`setStagePrField`/`readStagePr`; deliberately NOT in `INTENT_FIELDS`, like `draft_pr_*`), and flips it draft→ready at the stage gate (`haiku_await_gate` external_review block → `markPullRequestReady`, instead of opening a 2nd non-draft PR). **Mode shaping:** `discrete` opens one for EVERY stage; `discrete-hybrid` opens one ONLY for stages whose `review:` gate is/includes `external` (`stageRequiresExternalReview` — the continuous stages keep their work on the intent-main PR); continuous/autopilot/quick open none (single intent-main draft PR from intent_create). That stage PR is where the stage's runtime-verification proof uploads; non-PR stages upload to the intent-main PR (the dispatch builder's `proofTargetPrUrl` falls back automatically when `stage_prs[stage]` is absent). Idempotent: a stage revisit skips re-open when `stage_prs[stage].url` is set | git-worktree.ts (`openStageDraftPullRequest`), orchestrator/workflow/side-effects.ts (`openStageDraftPrIfDelivery`), orchestrator/studio.ts (`stageRequiresExternalReview`), tools/orchestrator/haiku_await_gate.ts (gate flip), state-tools.ts (`setStagePrField`/`readStagePr`/`readStagePrs`) |
| Proof gitignore | Quality Enforcement | Runtime-verification proof (`.haiku/intents/*/stages/*/proof/`, `.haiku/intents/*/proof/`, incl. the ephemeral Playwright driver under `proof/.driver/`) is gitignored — regenerated binary churn that bloats history and doesn't travel on merge; the verifier uploads it to the PR and the SPA serves it live off disk. `ensureHaikuGitignored` (was `ensureWorktreesGitignored`) seeds the worktree pool + both proof globs idempotently before any worktree lands; the engine's `git add` paths honor `.gitignore` so nothing more is needed to keep proof out of commits / out of `dirty_tree_blocking_tick` | state-tools.ts (`ensureHaikuGitignored`, `HAIKU_GITIGNORE_PATTERNS`), git-worktree.ts (callers), root `.gitignore` |
| Pre-tick clean-tree gate | Quality Enforcement | `haiku_run_next` blocks the tick when the working tree has uncommitted work OUTSIDE the engine's `.haiku/` bookkeeping and returns `dirty_tree_blocking_tick`, telling the agent to commit its own work with logical, story-telling messages (the engine no longer authors the agent's commits under a generic "wip" message). Runs after the mid-merge detector. Engine-owned `.haiku/` writes stay the engine's job (committed at lifecycle points; `autoCommitDirtyTree` fallbacks cover mid-merge edges). Classifier: `uncommittedAgentWork()`. Paired with the end-of-tick auto-push (active stage branch, or intent main during the completion phase) so CI + the delivery PR stay current | git-worktree.ts (`uncommittedAgentWork`, auto-push), tools/orchestrator/haiku_run_next.ts |
| Studio Fix Hat | Intent-Completion Review | Two-tier cascade (global → studio, more-specific wins; project `.haiku/` overrides plugin at each tier): `plugin/fix-hats/{hat}.md` → `plugin/studios/{name}/fix-hats/{hat}.md`. NOT per-stage. Dispatched against intent-scope feedback. The shared `validator` (terminal fix verifier) and `reconciler` (cross-stage alignment) live at the GLOBAL tier so every studio inherits ONE copy; a studio drops a same-named file to override (e.g. `hwdev` ships a hardware-specific `reconciler`). Chain ORDER comes from `resolveStudioFixHats` (STUDIO.md `fix_hats:` if declared, else alphabetical over the cascade) — the SAME ordered source the cursor, `haiku_feedback_advance_hat`, and `_reject_hat` all use | orchestrator.ts (`intent_completion_fix` action), studio.ts (`resolveStudioFixHats`), studio-reader.ts (`readStudioFixHatPaths`/`readStudioFixHatDefs`, `fixHatDirCascade`) |
| Feedback Triage | Quality Enforcement | `triaged_at:` on feedback frontmatter — pre-tick gate (`feedback-triage-gate.ts`) classifies open FBs before any handler dispatch; cross-stage findings are relocated via `haiku_feedback_move` rather than carrying a stage hint | state-tools.ts, orchestrator/workflow/feedback-triage-gate.ts |
| Stage Elaboration Guidance | Stages section | `plugin/studios/{name}/stages/{stage}/phases/ELABORATION.md` — stage-specific criteria guidance spliced ADDITIVELY into the elaborate/decompose prompt (it does not override any engine phase behavior). Read via `readPhaseOverride`/`readPhaseBody`; injected by the decompose builder. `EXECUTION.md` phase overrides were removed 2026-05-21 — they re-narrated engine mechanics (hat baton + full stage walk) into every hat dispatch, violating no-agent-mechanics-teaching | studio-reader.ts (`readPhaseBody`), orchestrator/prompts/stage/elaborate/decompose/index.ts |
| Long-lived repo knowledge (`scope: project`) | Stages section | A discovery template that declares `scope: project` (location under `.haiku/knowledge/`, repo-rooted) is **cross-intent knowledge that persists** — not produced-once-per-intent like `scope: intent` artifacts. The decompose builder injects a NON-blocking **read-as-prior** section ("Long-Lived Repo Knowledge") whenever such an artifact already exists: the agent reads it as a starting point and updates it in place ONLY if this intent's work shows it diverged. It is **not** a gate signal — the cursor gates discovery on existence, so forcing a refresh signal would re-emit every tick (the file still exists after a refresh). On first encounter (file absent) it's created via the normal discovery fan-out. `scope: project` artifacts: software `development/ARCHITECTURE`, `security/THREAT-MODEL`, `operations/RUNBOOK`, and (2026-05-26) the design-system `design/DESIGN-SYSTEM-ANCHOR` + `design/DESIGN-TOKENS`. `resolveArtifactPath` (and the drift witness/produced-set normalizers) resolve `.haiku/knowledge/…` against the repo root, NOT the intent dir | orchestrator/prompts/stage/elaborate/decompose/index.ts (read-as-prior section), studio-reader.ts (`resolveArtifactPath`, `readStageArtifactDefs` `scope`), cursor.ts (existence-only discovery gate) |
| Review Gate | Quality Enforcement | `review:` field in STAGE.md — `auto` (harness advances), `ask` (local human approval), `external` (blocks for external review system), `await` (blocks for external event), or compound list like `[external, ask]` (user chooses path) | orchestrator.ts |
| Reflection Dimension | Reflection phase | `plugin/studios/{name}/reflections/{dim}.md` — read by `readReflectionDefs(studio)` and injected as analytical lenses into the automatic intent-close `record_reflection` prompt. There is no manual reflect command; the engine runs the phase once before sealing | studio-reader.ts (`readReflectionDefs`), orchestrator/prompts/intent/reflection/record_reflection/index.ts |
| Completion Criteria | Throughout | `quality_gates:` in unit/intent frontmatter, harness-enforced | orchestrator.ts, quality-gate.sh |
| Stage `produces` (build vs knowledge) | Quality Enforcement | `produces: build` on STAGE.md (default absent = `knowledge`) marks a build/execution stage. On a build stage, `haiku_unit_write` requires any unit declaring non-empty `outputs:` to ALSO declare a `quality_gates:` field — *presence* not non-empty (explicit `[]` = deliberate "defers verification", missing key = invisible gap). Knowledge stages (research/distillation/design) carry no requirement. Enforced ONLY in `validateUnitFrontmatter` (write-time, pending-only `haiku_unit_write`); the cursor never revalidates, so in-flight active/completed units are never retroactively blocked. Surfaced on `StageConfig.produces`. Build stages tagged: software/development, libdev/development, gamedev/{prototype,production,polish}, hwdev/firmware. See `ARCHITECTURE.md` §4.3 | state-tools.ts (`validateUnitFrontmatter`, `haiku_unit_write` resolves via `readStageDef`), studio-config.ts + build-studio-config.ts (`produces`), decompose/index.ts (authoring directive) |
| Environment-blocked quality gates | Quality Enforcement | A quality gate that can't reach a live runtime dependency (DB down, Docker daemon off, a declared `requires_tool` absent) verified NOTHING — so it must NOT advance (false green) and must NOT file a `severity: blocker` code-fix FB (the fix loop has nothing to fix → the churn-to-bolt-cap bug). `classifyGateRun`/`classifyGateFailure` (`gate-environment.ts`) flag it via dependency-down output signatures (`connection refused`, `Cannot connect to the Docker daemon`, `could not connect to server`, …) OR an absent declared tool. `runInlineQualityGates` returns `quality_gate_environment_unavailable` (with `environment`/`env_reason`/`requires_tool`); `haiku_dispatch_quality_gates` routes those units to a `quality_gate_environment_blocked` action — stamps a `quality_gates_env_blocked` marker (NOT the approval, so the stage can't advance), files no blocker FB. The response tells the agent to **best-effort boot** the project's declared services (`.haiku/boot.md` `service:` processes) when the tool is live (`capabilities.isToolAvailable`), then re-run; if it can't (tool absent / boot failed / no recipe), **escalate to the user and HOLD** (`ENV_BOOT_ATTEMPTS = 1` flips boot→escalate). Doctrine: `plugin/prompts/_shared/service-dependencies.md`, injected into the terminal-hat execute dispatch. Mirrors the runtime-verifier's `BLOCKED` verdict for gates | gate-environment.ts, capabilities.ts, state-tools.ts (`runInlineQualityGates`, `serviceToolsForIntent`), tools/orchestrator/haiku_dispatch_quality_gates.ts (`stampUnitGateEnvBlock`, `bootGuidance`), _shared/index.ts (`service-dependencies`) |
| Ambient tool capability probe | N/A — implementation | `capabilities.ts` — the project-tool analog of `harness.ts`. A registry of ambient tools (`docker`, `docker-compose`, `make`) each with a memoized LIVENESS probe: `docker` runs `docker info` (daemon round-trip), NOT `docker --version` (binary-present ≠ daemon-up). `isToolAvailable(name)` drives "can the agent boot this declared service, or must it escalate?" `_resetCapabilityCacheForTests` clears the per-process cache | capabilities.ts |
| Canonical intent stage plan | Profiles section | `intent.stages` is the CANONICAL, materialized, ordered stage plan — the studio's `stages:` is the superset template; the intent owns the live list (FSM-driven, stamped at `haiku_select_mode`, mutated only by the engine). `resolveIntentStages` returns `intent.stages` filtered by a stale-stage guard (a stage the studio no longer defines is dropped), falling back to the full studio list when absent. The old `skip_stages` deny-list was REMOVED 2026-05-27 — one canonical list; "what got dropped" = `studio.stages − intent.stages`. Migration `v9-to-v10` strips `skip_stages` + backfills `stages` (honoring the old deny-list inline) | orchestrator/studio.ts (`resolveIntentStages`), state/schemas/intent.ts (FSM list), orchestrator/migrations/v9-to-v10.ts |
| Optional stage + drop | Profiles section | `optional: true` on STAGE.md marks a droppable stage. The cursor's FIRST arrival at an optional, unstarted stage (no `elaboration.md`, no units) sets `optional_offer` on the `elaborate_loop` action; the elaborate prompt leads with a keep-or-drop decision + a "what you're severing" dependents summary (`computeStageDependents`). Drop → `haiku_drop_stage { intent, stage }` removes it from `intent.stages` (guards: `drop_stage_not_active` / `_not_optional` / `_already_started`); keep → just elaborate (one-shot — starting elaboration flips the condition off, no FM bookkeeping). Cross-stage refs to a dropped stage auto-ignore: the coverage gate skips a stage with no dir, `start_unit`/`decompose` inject only `.exists` inputs, and `decompose` filters by plan (`filterInputsByPlanStages`). software's `design`/`operations`/`security` are optional; the MVP core `inception`/`product`/`development` is mandatory | orchestrator/workflow/cursor.ts (`optional_offer`), orchestrator/studio.ts (`resolveStageOptional`, `computeStageDependents`, `filterInputsByPlanStages`), tools/orchestrator/haiku_drop_stage.ts, prompts/stage/elaborate/elaborate_loop/index.ts, studio-config.ts (`StageConfig.optional`) |
| Deprecated studio | Profiles section | `deprecated: true` on STUDIO.md hides a studio from new-intent pickers (`haiku_select_studio`) and `haiku_studio_list`, but `resolveStudio` still resolves it by name so in-flight intents on it finish unchanged. Deprecate — never delete — a retired studio. `libdev` folded into `software` (appdev) 2026-05-27 via the optional `release` stage and is deprecated | studio-config.ts (`StudioConfig.deprecated`), tools/orchestrator/haiku_select_studio.ts, state-tools.ts (`haiku_studio_list`) |
| Backpressure | Principles section | Quality gates enforced by harness, not agent | quality-gate.sh, orchestrator.ts |
| Operating Modes | Operating Modes section | interactive=HITL, /haiku:haiku-pickup=OHOTL, /haiku:haiku-autopilot=AHOTL | prompts/core.ts, prompts/complex.ts |
| Hard Gates | Execution phase | exit code enforcement in quality-gate.sh | orchestrator.ts |
| Persistence | Context Preservation | Environment-detected via `isGitRepo()` (git or filesystem) | state-tools.ts, git-worktree.ts |
| Providers | Memory Providers section | `plugin/schemas/providers/*.json`, `plugin/providers/*.md` | config.sh |
| Provider OAuth (auth + token store) | N/A — implementation | Git-provider (github/gitlab) auth brokered through **haikumethod.ai** (NOT .com) — the Cloud Function in `deploy/auth-proxy/` runs a brokered authorization-code handshake (NOT the provider's native RFC-8628 device flow): `/cli/start` mints a **`session_id`** + `verification_url`, the browse-site callback POSTs the exchanged token to `/cli/complete`, the CLI polls `/cli/poll { session_id }` and reads the token bundle **spread at top level** of the `ready` response (matches `deploy/auth-proxy/src/cli.ts`). Token stored client-only in `~/.haiku/settings.json`. **Auth-when-needed (NOT auth-first):** `ensureProviderToken(provider)` (in `haiku_auth_login.ts`) returns a usable stored token or runs the handshake INLINE — the engine never tells the agent to "call haiku_auth_login first." Provider is the repo's (origin host). Tools: `haiku_auth_login` (explicit login), `haiku_auth_status`, `haiku_auth_logout`, `haiku_upload_proof` (upload proof to the delivery PR/MR over REST — auto-auths via `ensureProviderToken`; `proof_upload_auth_unavailable` only when auth genuinely can't be obtained). Token shape (`access_token`/`refresh_token`/`expires_at`/`scopes`/`account`/`host`) + read/write/clear live in `global-settings.ts`; provider detection (`parseGitRemote`/`providerFromHost`/`providerFromOrigin`/`readOriginRemoteUrl`) in `git-worktree.ts` | global-settings.ts, state/schemas/global-settings.ts, tools/orchestrator/haiku_auth_*.ts + haiku_upload_proof.ts, deploy/auth-proxy/ |
| PR/MR ops via stored token (Phase 4) | N/A — implementation | The engine drives PR/MR **create** + **mark-ready** over the provider REST API (`provider-rest.ts`, injectable fetch), **authenticating when needed**: `resolvePrRestContextEnsuringAuth` (origin host → provider → `ensureProviderToken`) obtains a token inline if none is stored — no "auth first." Any REST miss (incl. auth that couldn't be obtained — broker down / declined / headless) falls back to the `gh`/`glab` CLI (`openPullRequestCli`). The pre-open guard skips only when there's no CLI AND no recognized provider remote (`providerFromOrigin`). NO merge over REST — the human's merge is the approval signal (merge stays CLI/human-only). The two synchronous handler entry points (intent-main draft open in `haiku_intent_create`, repair PR in `haiku_repair`) stay CLI-only — a sync handler can't await REST/auth. REST contracts are doc-derived + mock-validated; CLI is the integration-proven path | provider-rest.ts, git-worktree.ts (`openPullRequest`/`markPullRequestReady` async + `resolvePrRestContextEnsuringAuth`), orchestrator/workflow/side-effects.ts |
| Closing brief (BRIEF.md) | Quality Enforcement | Per-stage user-facing `BRIEF.md` written twice via the engine-owned `haiku_write_brief { body }` tool — `pre` (the plan) on first write, `post` (what shipped) on the closing rewrite at stage finish. The tool takes ONLY the body; the engine resolves intent (from branch), stage (from cursor), and the `phase:` frontmatter (file absent → pre, present → post — the same signal `stageOwesClosingBrief` gates on, so frontmatter can't drift from the cursor). Frontmatter via gray-matter. The `write_brief` cursor action fires from `stageOwesBrief` (pre, BRIEF absent) and `stageOwesClosingBrief` (post, BRIEF exists + `phase != post`); two reachable surfaces (non-autopilot user-gate, autopilot/merge in `haiku_run_next`). Opt out with `brief: false` on intent FM | tools/orchestrator/haiku_write_brief.ts, orchestrator/workflow/cursor.ts (`stageOwesBrief`/`stageOwesClosingBrief`), prompts/stage/review/write_brief/ |
| Harness | N/A (implementation detail) | `--harness <name>` MCP arg or `HAIKU_HARNESS` env var; capability registry in `harness.ts`, instruction adaptation in `harness-instructions.ts` | harness.ts, harness-instructions.ts, orchestrator.ts, server.ts |
| Architecture (canonical) | N/A — implementation reference | `plugin/studios/ARCHITECTURE.md` — boundaries, lifecycle, hat patterns, FB-as-unit fix-loop semantics. Read before any structural change to studios, stages, hats, or workflow tools | ARCHITECTURE.md |
| Workflow-managed file boundary | Quality Enforcement | PreToolUse hook denies generic Read/Write/Edit on `units/*.md`, `feedback/*.md`, `intent.md`, `stages/*/state.json`. Agents go through MCP tools only; redirect messages name the right tool | hooks/guard-workflow-fields.ts |
| Unit CRUDL (MCP) | N/A — implementation | `haiku_unit_write` (create/rewrite, FM validators, DAG cycle detection, pending-only lifecycle), `haiku_unit_read` (body+title only — no FM exposed), `haiku_unit_get` (read ONE agent-authorable/corrective FM field — `quality_gates`/`outputs`/`inputs`/`depends_on`/`model`/`closes`/`title`; refuses FSM-driven fields with `unit_field_engine_only`; the read half of the corrective exemption so a gate-command fix is read→modify-one→write-back, not a whole-array clobber), `haiku_unit_set` (FM field update, lifecycle-enforced; `outputs`/`quality_gates` stay editable after a unit goes active — corrective exemption), `haiku_unit_delete` (pending only), `haiku_unit_list` | state-tools.ts |
| Feedback CRUDL (MCP) | N/A — implementation | `haiku_feedback_write` (body update, lifecycle-enforced), `haiku_feedback_read` (body+title only), `haiku_feedback` (create), `haiku_feedback_reject` (mark invalid/stale — stamps `rejected_at` so the open-feedback walk treats it as terminal; the v4 `haiku_feedback_update` status-transition tool was REMOVED — closure runs through the fix-loop's terminal hat `haiku_feedback_advance_hat`, and a stale `haiku_feedback_update` call returns `feedback_update_removed_in_v4`), `haiku_feedback_delete`, `haiku_feedback_list`, `haiku_feedback_set_targets` / `haiku_feedback_set_severity` (classifier-hat backfill — write-once) | state-tools.ts |
| Feedback severity | Quality Enforcement | `severity:` on feedback frontmatter — `blocker` \| `high` \| `medium` \| `low` (`FEEDBACK_SEVERITIES` in `state/schemas/feedback.ts`). **Required** on the agent-facing `haiku_feedback` create tool (review agents classify as they file). User/SPA findings land severity-less (the `writeFeedbackFile` HTTP path omits it) and the `classifier` fix-hat backfills via `haiku_feedback_set_severity` (write-once, `severity_already_set` on re-call). The fix-loop dispatches highest-severity-first: `feedbackSeverityRank` orders the initial pool in `collectFeedbackDispatches` (cursor.ts) AND the slot-replenishment pick in `pickUndispatchedFbBlock` (state-tools.ts) — unclassified ranks as `medium`. Engine-authored FBs set explicit severity at write time (gate-blockers → `blocker`, drift → `high`). Surfaced on the wire (`FeedbackSeveritySchema`) + SPA badge (`FeedbackItem.tsx`) | state/schemas/feedback.ts (`FEEDBACK_SEVERITIES`, `feedbackSeverityRank`), state-tools.ts (`haiku_feedback_set_severity`, `pickUndispatchedFbBlock`), orchestrator/workflow/cursor.ts (`collectFeedbackDispatches`) |
| Severity-gated fix-loop activation | Quality Enforcement | A finding is **blocking** (triggers a fix wave AND holds the stage gate) iff it's unclassified (null severity — the classifier must run) OR `feedbackSeverityRank(severity) <= fixSeverityThresholdRank()`. Threshold defaults to `high` (blocker+high block; medium+low ride along), overridable via **`HAIKU_FIX_SEVERITY_THRESHOLD`** env var (read at call time). `collectFeedbackDispatches` returns null (no wave) unless some open FB is `inFlight` OR blocking — so a lone reviewer nit never spins a worktree; the stage just advances and the nit stays open + advisory. Once a blocker DOES open a wave, the whole open set dispatches together (severity-ordered), sweeping the ridealong lows in the same pass. Agent findings carry severity at creation, so a reviewer's stream of `low`s is non-blocking from tick 1 | state/schemas/feedback.ts (`fixSeverityThresholdRank`, `isFixBlockingSeverity`), orchestrator/workflow/cursor.ts (`collectFeedbackDispatches` wave-active gate) |
| Feedback anti-churn (re-review) | Quality Enforcement | Two LIVE, reword-resistant mechanisms keep reviewers from re-litigating settled findings. **Preventive:** `buildExistingFeedbackBlock` hands every review/approval/intent-review subagent ALL prior findings on scope — incl. closed/rejected — now with `severity`, the closure_reply (`resolved: …`) and reject reason (`dismissed: …`), and tells it to audit the delta / respect the dismissal, not re-file. **Detective:** `detectSettledDuplicate` (create-time, Jaccard token-overlap ≥ 0.6 on same target_unit) returns a `duplicate_warning` on the `haiku_feedback` response when a finding restates an already-closed/rejected one — advisory (a bad fix can legitimately re-raise), pointing the agent at the prior resolution + `haiku_feedback_reject`. The dispatch_approval prompt also tells the reviewer its units came back because the WORK changed (only changed units re-sent — cursor filters `!approvals[role]`), so audit the fix delta. NOTE: the old `computeFeedbackSignature` stage-iteration loop detector is dormant in v4 (`appendStageIteration` never gets `feedbackTitles`); the per-FB bolt cap remains the hard backstop | _helpers.ts (`buildExistingFeedbackBlock`), state-tools.ts (`detectSettledDuplicate`, `findingSimilarity`), prompts/stage/approve/dispatch_approval/subagent.eta.md |
| FB-as-unit fix loop | Fix Loop / architecture §5–6 | Fixer hats LAND THE ACTUAL FIX — real code/test/artifact changes on disk, isolated in the chain's worktree (see Worktree isolation below) — and record the resolution in the FB body via `haiku_feedback_write`. The flagged unit's SPEC file is read-only (`haiku_unit_read`); its code outputs are patchable. Hat progression via `haiku_feedback_advance_hat` / `haiku_feedback_reject_hat` (mirrors of unit equivalents). The workflow auto-closes the FB when the last hat in `fix_hats:` advances, merging the chain's code into the base branch. Closed FBs also inform the next elaborate iteration for any follow-up units, but the immediate fix is the fixer hat's deliverable, not deferred (corrected 2026-05-21 — ARCHITECTURE.md §6.2 previously claimed fix-loops only diagnose; the shipped `fix-hats/builder.md` mandate is authoritative: "a plan, a diagnosis, or a description of the fix is not the fix") | state-tools.ts (`haiku_feedback_advance_hat`, `haiku_feedback_reject_hat`), orchestrator.ts (`review_fix`, `intent_completion_fix` dispatches) |
| Worktree isolation | architecture §1.4 | Each unit hat-loop AND each fix-chain hat-loop runs its CODE in a per-loop worktree on an ephemeral branch (`haiku/<slug>/<unit>` or `haiku/<slug>/fix-<scope>-<FB>`, forked from the base branch); workflow state (FM iterations/reviews/approvals, FB body) stays on the base branch via engine MCP tools (cwd-independent). Created at dispatch (`createUnitWorktree`/`createFixChainWorktree`), pushed on each advance (`pushUnitWorktree`/`pushFixChainWorktree`), merged + reaped (local AND remote) at terminal under `withStageLock` (`mergeUnitWorktree`/`mergeFixChainWorktree`). Conflicts → `resolve_merge_conflicts` (units) / `integrate_fix_chains` (fix-chains, completed by the pre-tick `completePendingFixChainMerges` gate). Pickup recovery: recreate-from-remote, or `resetLostUnits` clears iterations when worktree+branch are gone (gated on `hasGitRemote()`). Rejected fix-chains discard their code (`cleanupFixChainWorktree`). All git-layer/run-tick — the cursor reads none of it (Rule 1); no-op in filesystem mode. **Engine-state merge guard (`restoreEngineStateFromBase` / `engineProtectedMergeInCwd`): EVERY `.haiku`-bearing merge re-asserts engine-owned state (units/feedback/intent.md) from the authoritative side so a silent 3-way auto-resolve can't revert rich frontmatter to a stale/skeleton snapshot. Covers unit merge, fix-chain merge, discovery merge (target = stage, authoritative), AND the per-tick `syncBranchDownstream` (mainline→intent-main→stage; target always ahead). Without it the stage's units silently reverted to the v3 `status:`+title skeleton every tick — the skeleton's `status:` re-fired the migrator (`migrated` every tick) and reverted closed fixes. Bug report 2026-05-23.** | git-worktree.ts (`restoreEngineStateFromBase`, `engineProtectedMergeInCwd`, `mergeUnitWorktree`/`mergeFixChainWorktree`/`mergeDiscoveryWorktree`/`syncBranchDownstream`), orchestrator/workflow/run-tick.ts (`resetLostUnits`, `completePendingFixChainMerges`), orchestrator/workflow/unit-branch-recovery.ts, orchestrator/workflow/fix-chain-merge-gate.ts |
| Verifier hat | architecture §3 (plan-do-verify) | Terminal hat in every per-unit hat sequence. Body-only mandate (FM is workflow-engine territory). Validates substance, citation, internal consistency, decision-register consistency. Calls `haiku_unit_advance_hat` on pass, `haiku_unit_reject_hat` on fail | plugin/studios/{studio}/stages/{stage}/hats/verifier.md |
| View session (MCP) | N/A — implementation | `haiku_view` (open tunneled URL — boot mode spawns the project's dev/start script on an ephemeral port, viewer mode points at the SPA artifact-browser route at `/view/<sessionId>?artifact=<path>&stage=<stage>`; mode auto-detects with explicit `mode: "boot"`/`"viewer"` overrides), `haiku_view_close` (explicit shutdown of view session + spawned dev server). Lifecycle: explicit close, 30min TTL, tick-scoped orphan sweep via `killAllOrphanedBootSessions` in `run-tick.ts`. Consumed by any review-agent (or other tool-using agent) that wants to drive a real browser at a rendered artifact or a booted app; per-studio mandate files (e.g. `runtime-verifier.md`) are where studios opt into using it. | view-boot.ts, sessions.ts (`ViewSession`), http/session-routes.ts (`/view/:sessionId`), http/file-serve.ts (extended `/stage-artifacts/:sessionId/*` to allow view sessions). HTML artifacts are never served as renderable `text/html` (FB-21 octet-stream + attachment); the serve layer inlines adjacent local stylesheets (`serveArtifact` in `path-safety.ts` → `inlineAdjacentStylesheets` in `html-inline.ts`, shared with `parser.ts`) and ViewPage fetches the bytes + renders via a sandboxed `srcDoc` iframe (`HtmlRenderer`), so a wireframe authored as `index.html` + `styles.css` styles correctly. Same inliner backs the SPA review surface (StageReview srcDoc). Embedded relative refs (`<img src>`, `srcset`, CSS `url(…)`) are then rewritten to authed `/stage-artifacts/<sid>/…?t=<jwt>` tunnel URLs by `resolveEmbeddedAssetUrls` (haiku-ui `pages/review/shared/inline-asset-urls.ts`, applied in both StageReview + ViewPage) so raster images load inside the srcDoc; inlined-CSS `url(…)` is re-based onto the HTML's dir server-side (`rewriteCssUrls` in `html-inline.ts`) so the client resolver is uniform. `.js`/`.css`/`.svg`/font sub-resources stay octet-stream-blocked (FB-21) — scripts never execute, SVG-as-img / custom fonts degrade to fallbacks. |
| Prompts cascade | N/A — implementation | Engine prompt templates (`.eta.md`) live at `plugin/prompts/<rel>/<name>` (single source of truth, shipped with the plugin). `loadTemplate` in `_load-template.ts` resolves them through a two-tier cascade: project override at `.haiku/prompts/<rel>/<name>` wins over plugin default at `<pluginPromptsRoot>/<rel>/<name>`. Build-time `canonicalize-prompt-templates.mjs` esbuild plugin rewrites `loadTemplate(import.meta.url, …)` call sites in the TS builders under `packages/haiku/src/orchestrator/prompts/` to a `"@canon:<rel>"` sentinel form — no copy step, the bundle just needs sentinel-form lookups to avoid reading its own `import.meta.url` at runtime. Replaces the older inline-string + copy-to-`plugin/templates/` approach so teams can override engine prompts locally without forking the plugin, and the plugin tree stays the canonical home for prompt bodies. | packages/haiku/src/orchestrator/prompts/_load-template.ts (loader cascade), packages/haiku/scripts/canonicalize-prompt-templates.mjs (build-time sentinel rewrite + existence check), plugin/prompts/ (source of truth), plugin/package.json (`files: [...]` includes `prompts`) |
| Observations + reflection | N/A — implementation | Default-on reflection loop. At each stage close, cursor emits `record_observations` so the agent writes a free-form `.haiku/intents/<slug>/stages/<stage>/observations.md` capturing out-of-band churn (mandate ambiguity, engine friction, surprises) the FB stream / outputs / iterations don't already show. At intent close, cursor emits `record_reflection` so the agent reads all stage observations + FBs + iterations + outputs and writes a synthesized `.haiku/intents/<slug>/reflection.md`, lands any override-class findings as project overlays directly under `.haiku/...` (cascade picks them up next tick, no rebuild), and reports engine-class findings via the existing `haiku_report` tool (Sentry pipeline, message prefixed `[autotune engine-class]`). The studio's `reflections/*.md` dimension files are injected into this prompt as analytical lenses (via `readReflectionDefs` in the builder) — they structure the synthesis when present, generic sections otherwise. This automatic phase is the ONLY reflection path: the standalone `/haiku:haiku-reflect` skill + `haiku_reflect` tool were removed (consolidated here). All changes commit with an `autotune:` prefixed message — PR review is the human gate, no FB queue, no tune dashboard. Opt-out via `reflection: false` on intent.md FM (backward-compat: `autotune: false` still respected). | prompts/stage/complete/record_observations, prompts/intent/reflection/record_reflection, cursor.ts (`isReflectionEnabled`) |
| Playwright MCP | N/A — bundled tool | `@playwright/mcp` shipped in `plugin/.mcp.json` under the server name **`haiku-playwright`** (prefixed to avoid colliding with a user's separately-installed Playwright MCP) with `--headless` always-on so dispatch is non-interactive. Available to every agent. As of the script-driver change it is the runtime-verifier's **fallback** driver (when `@playwright/test` can't be self-installed) — the primary web/GUI driver is now a self-installed Playwright script (see Runtime-verification doctrine). Per-studio mandates decide how else to use it. | plugin/.mcp.json |
| Runtime-verification doctrine | N/A — implementation | Harness-agnostic "verification = runtime observation" doctrine — the cross-harness analog of Claude Code's built-in `verify`/`run` skills (which only reach CC agents). Authored once as the `runtime-verification` shared block (`plugin/prompts/_shared/runtime-verification.md`, registered in `_shared/index.ts`); the dispatch builders inject it via `sharedBlockRef("runtime-verification")` ONLY for `RUNTIME_OBSERVATION_ROLES` (currently `runtime-verifier`) in `dispatch_approval` (post-execute) and `intent_review` (intent-completion). `RUNTIME_OBSERVATION_ROLES` + `PR_INTERACTION_ROLES` were extracted (2026-05-22) to the neutral leaf `orchestrator/review-role-classes.ts` (re-exported from `_shared/index.ts` so the builders' import surface is unchanged) so the workflow role-list builder can read the classification without importing the Eta-laden prompt registry. **The same set gates the PRE-execute review walk: `stageRoleLists` in `workflow/cursor.ts` EXCLUDES `RUNTIME_OBSERVATION_ROLES` from `reviewRoles` (there's no built work to observe before execution) while keeping them in post-execute `approvalRoles`.** Because the doctrine rides the engine-built prompt (not a CC skill), it reaches every harness. Routes by surface — web/GUI → `haiku_view` boot returns the URL, then the agent writes a **self-installed Playwright script** (own scratch dir, never the project's deps; `haiku-playwright` MCP is the fallback) that **records video** + step screenshots into the gitignored `proof/`; CLI → run the command; server → the socket; library → the public export; none → SKIP — and bans tests/typecheck-as-verification. `runtime-verifier` is now in BOTH `RUNTIME_OBSERVATION_ROLES` AND `PR_INTERACTION_ROLES` — the dispatch builders compute `prInteraction` + a mode-aware `proofTargetPrUrl` (discrete/discrete-hybrid → the stage's `stage_prs[stage].url`; else the intent-main `draft_pr_url`) and the subagent templates grant ADDITIVE scopes (proof-write AND PR-upload), so the verifier uploads its gitignored proof to the relevant PR (GitLab uploads API / GitHub release-asset). The intent-review + dispatch_approval subagent no-write scopes are carved out for both grants. | orchestrator/review-role-classes.ts (`RUNTIME_OBSERVATION_ROLES`, `PR_INTERACTION_ROLES` — runtime-verifier in both), prompts/_shared/index.ts (re-export, `sharedBlockRef`), workflow/cursor.ts (`stageRoleLists` pre-execute exclusion), prompts/stage/approve/dispatch_approval/index.ts (`prInteraction`, `proofTargetPrUrl`), prompts/intent/review/intent_review/index.ts, plugin/prompts/_shared/runtime-verification.md, plugin/providers/git.md (upload doctrine) |
| Project boot recipe | N/A — implementation | `.haiku/boot.md` at the project root — the harness-agnostic analog of `run-skill-generator`. Frontmatter is the machine-readable boot recipe (`command:` single-process, or `processes:` + `primary:` multi-process, plus `cwd`/`env`/`ready_url`/`port_env`); the markdown body is human/agent drive notes the runtime-verifier reads. `readBootRecipe(projectRoot)` in `view-boot.ts` normalizes either form to a `BootProcessSpec[]` group (a single `command:` becomes a one-process group named `app`) so all boots share one supervisor with uniform env/readiness/port handling. `haiku_view` boot precedence: agent-supplied `command`/`processes` → `.haiku/boot.md` recipe → `package.json` dev/start auto-detect. Throws `haiku_view_boot_recipe_invalid` on a structurally-broken recipe; null (auto-detect fallthrough) when absent or notes-only. A process may be marked `service: true` + `requires_tool: docker` to declare a service DEPENDENCY (DB/queue/cache) the app + its quality gates need — `readServiceProcesses(projectRoot)` returns those (the gate runner brings them up before a service-needing gate; see "Environment-blocked quality gates"); a service is never the Playwright `primary`. | view-boot.ts (`readBootRecipe`, `readServiceProcesses`, `BootProcessSpec.env`/`.service`/`.requires_tool`), server/tool-call.ts (haiku_view boot path) |

## H·AI·K·U Terminology (CRITICAL)

| H·AI·K·U Term | Agile Equivalent | Description |
|---|---|---|
| Intent | Feature / Epic | The overall thing being built |
| Unit | Ticket / Story | A discrete piece of work within an intent |
| Bolt | Sprint | The iteration cycle an agent runs within a unit |
| Studio | (no equivalent) | A named lifecycle template (profile implementation) containing stages |
| Stage | (no equivalent) | A lifecycle phase within a studio, containing hats and review gates |
| Hat | Role | A behavioral role scoped to a stage, defined in `hats/{hat}.md` files within the stage directory |
| Review Gate | Quality Gate | A checkpoint between stages controlling advancement. `auto` = harness-only (no human). `ask` = local review UI, human approves/rejects via MCP response. `external` = blocks until external system (GitHub/GitLab) approves; signal detected primarily by branch merge detection, with URL-based CLI probing as fallback. `await` = blocks until an external event occurs (not a review — e.g., customer response, pipeline). Compound: `[external, ask]` = user chooses between external submission or local approval. |

### Hierarchy

```
Studio > Stage > Unit > Bolt
```

- **Studio** is NOT the same as Stage. Studio = the lifecycle template. Stage = a phase within it.
- **Unit** is NOT the same as Bolt. Unit = the work itself. Bolt = the iteration cycle within a unit.
- **Hat** is always scoped to a Stage, defined in `stages/{stage}/hats/{hat}.md` files. Project-level augmentation: `.haiku/studios/{studio}/stages/{stage}/hats/{hat}.md`.

## Version Management

- Plugin version in `plugin/.claude-plugin/plugin.json` -- auto-bumped by CI
- Changelog follows Keep a Changelog format at repo root
- Website deploys on push to main when `website/` changes
