---
name: feedback
description: Submit feedback or a bug report to the H·AI·K·U team
---

# Feedback

Ask the user what feedback they'd like to share, then call `haiku_feedback` with their `message`. Optionally include `contact_email` and `name` if the user provides them.
