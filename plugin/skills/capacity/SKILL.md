---
name: capacity
description: Historical throughput — bolt counts, stage durations, patterns
---

# Capacity

Call `haiku_capacity` to get throughput metrics. Optionally pass a `studio` filter. Present the result to the user.
